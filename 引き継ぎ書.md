# SSG学習アプリ 引き継ぎ書

> 別チャットで続きを作るとき、このファイルの内容を最初に貼ると文脈が引き継げます。
> 最終更新: 2026-08-08。■当セッション(2026-08-08)の変更（すべて公開済み・SQLも適用済み）:
> - **古文に「歴史的仮名遣い」を追加（65問・入力式）**。国語→古文→歴史的仮名遣い（分野の先頭）。歴史的仮名遣いを**現代仮名遣いに直してひらがなで入力**させる。実際のテストが記述式で、4択だと消去法で当たってしまうため入力式にした
>   - 内訳: 語中語尾のハ行14／ゐゑを13／ぢづ10／くわぐわ5／母音の連続13／む→ん4／教科書の一文6（竹取・枕草子・徒然草・平家・おくのほそ道）
>   - **別実装のルールエンジンで機械照合**: `node tools/check_kana.mjs`。くわぐわ→拗音→ぢづ→ゐゑを→ハ行→母音連続 の順に変換して答えと突き合わせる。**65問中63問が自動一致**
>   - 残り2問（「はるはあけぼの」「つきひははくたいの…」）は**助詞の「は」と複合語**のため機械変換が当てはまらない＝`manual:true`＋理由を書いて照合対象外にし、実行時に一覧表示される（塾長が目で確認する用）。この2問は「直さない場所がある」ことを教える良問なのであえて残した
>   - データ側の指定で照合できる仕組み: `post:"mu-n"`（助動詞むを機械的にんへ）／`yoon:["じや"]`（「う」が続かない拗音を明示。「なりゆく」の「りゆ」を拗音と誤判定しないため）
>   - ⚠️ルールエンジンが**私のデータ誤りを1件検出**（「なからむ」の答えを「ならん」と書いていた→「なからん」に修正）。⚠️「たまふ」は現代仮名遣いが「たまう」か「たもう」か中学教材で割れるため**あえて入れていない**
>   - 登録=`node tools/register_kana.mjs`（`--replace`で入れ直し）。元データ=`国語CSV/古文_歴史的仮名遣い.json`。format='input'・grade=''・unit='歴史的仮名遣い'
>   - index.html: `KANA_UNIT`/`isKanaInput()`を追加し入力式に分岐。qAskは「現代仮名遣いに直して入力」、誤答表示は「正しい書き方：」。**送り仮名の省略を許すのは漢字（学年あり）だけ**にした（歴史的仮名遣いは完全一致）。カタカナで打っても`toHira()`で正解になる
>   - **解説パネルを全教科対応に**（従来は数学だけ）。`afterAnswer`を「間違えた＋解説がある問題なら出す」に一般化し、パネルの色をCSS変数`--exp`で教科色に追従させた（数学=青／国語=桃）。※解説を持つのは現状 数学395問と歴史的仮名遣い65問だけなので他教科の挙動は不変
>   - 実機検証済み: 10問通し（カタカナ入力・前後空白・2問わざと誤答）→**サーバー採点7/10で画面の○×と完全一致**
> - **数学に中1・中2を追加（計8単元259問／数学は全学年で12単元395問に）**。中3と同じ方針＝計算コアだけ、思考問題は入れない
>   - 中1: 正負の数40(入力)・文字式32(4択)・一次方程式30(入力)・比例と反比例30(入力)
>   - 中2: 式の計算30(4択)・連立方程式32(入力)・一次関数30(入力)・角の計算35(入力)
>   - 生成=`node tools/gen_math12.mjs`（中3は別ファイル`gen_math3.mjs`のまま）。登録=`node tools/register_math.mjs`（**数学CSV/<学年>_<単元>.json を全部自動で拾う**ように書き換え。`--replace`で入れ直し、`中1`のように学年指定も可）
>   - **検証器を全学年対応に**（`tools/check_math3.mjs`→**`tools/check_math.mjs`にリネーム**）。多変数の恒等式・累乗・`÷`の優先順位・漢数字(三角形/正八角形)に対応し、**全395問エラー0**
>   - 検証モードを追加（各問の`check.mode`。省略時は問題文から自動判定）: expr/roots/linsys/fn/line/points/ngon/calc。詳細は check_math.mjs 冒頭のコメント
>   - `toMath()`を拡張: 「x=3」「y=-2」の頭を落とす／「180度」「45°」の単位を落とす（角の計算で生徒が単位を付けても正解にする）
>   - `MATH_INPUT_UNITS`に入力式の単元を追加（正負の数・一次方程式・比例と反比例・連立方程式・一次関数・角の計算・二次方程式）
>   - 実機検証済み: 全12単元が10問ずつ出題・解説つき。角の計算を10問通し（表記を`360度`/全角`１８００`/`2340°`/空白つき とバラバラにし2問わざと誤答）→**サーバー採点8/10で画面の○×と完全一致**
>   - ⚠️検証器が実バグを2件検出: ①`24ab÷-6a`とカッコ無しで出力していた（教科書記法は`÷(-6a)`）②中2の`-1(2x+5y)`の係数1が省略されていなかった。どちらも修正済み
> - **数学を開通（中3の計算4単元・全136問）**。方針=**アプリは計算の速さ／思考問題はノートと授業**。思考問題（証明・文章題・関数の応用）はアプリに入れない
>   - 単元と形式: **式の展開36問・因数分解36問・平方根32問＝4択**（誤答は「ありがちな計算ミス」を人手で設計＝符号ミス/中央項忘れ/和と積の取り違え等）／**二次方程式32問＝数値の入力式**（答えは必ず整数。√やπの入力が要らない範囲に限定）
>   - **問題はスクリプトで自動生成**：`node tools/gen_math3.mjs` → `数学CSV/中3_*.json`。写真もOCRも要らず、答えは構成上つねに正しい（漢字の「読みは要照合」のような不安がない）
>   - **検証もスクリプト**：`node tools/check_math3.mjs` が全問を**数値代入で独立検証**（問題文の式と正解が恒等的に等しいか／誤答が正解と同値になっていないか／入力式は解を代入して0になり「小さい方・正の方」等の指定と合うか）。136問エラー0
>   - 登録: `node tools/register_math.mjs`（`--replace` で入れ直し）。format='choice'/'input'、grade='中3'、unit=単元名
>   - index.html: SUBJECTS.math を `on:true, gradeFirst:true, units:{中1:[],中2:[],中3:[4単元]}, unitNoun:'単元'`。英語と同じ showGradeList→showUnits を再利用（中1中2は「準備中」表示）
>   - **入力式の正規化 `toMath()`**（漢字の`toHira()`にあたるもの）: 全角→半角・空白除去・先頭の`x=`を落とす・マイナスに見える記号（−–—ー―‐‑ｰ）をすべて`-`に統一。サーバーは文字列完全一致で採点するので**正規化した値を送って画面の○×と加点を一致させている**
>   - **解説（考え方）パネルを新設**。数学で**間違えたときだけ**途中式を出し、**「つぎへ」を押すまで待つ**（自動で流れない）。正解時は従来どおり自動で進む
>   - クイズ画面の文言を数学用に差し替え（「ノートで計算してからえらぼう」「ノートに途中式を書いてから答えよう・時間制限はありません」）
>   - 実機検証済み: student07でログイン→数学→中3→二次方程式を10問（うち3問わざと誤答）→**サーバー採点が7/10で画面の○×と完全一致**・参加賞+1と6問以上正解+1が加算・進捗バッジも7/32に反映
> - **`migrate_math.sql` 適用済み**（2026-08-08・塾長がSQL Editorで実行）。内容=①`subjects`のmathを`is_active=true`に ②`get_daily_questions`が`explanation`も返すように。公開URLで解説パネルが出ることを確認済み
> ■以前のセッション(2026-08-07)の作業（コード変更なし）:
> - **ログイン基盤の健全性を確認**: `get_login_email` RPCが SSG01〜SSG10・student07 の全IDで正しく認証メールを返すこと、SSG01/PIN123456 で実ログインできることをAPIで検証済み。在籍状況も確認（はるかSSG10のみ退塾）
> - **生徒への配布物を作成**（§14-1）。PINを含むので**publicリポジトリには入れず** `/Users/takuya_oohashiba/Desktop/SSGログイン配布/` に置いた:
>   - `配布文面.txt` … LINE等にそのまま貼れる文面。A=既にアプリを使っている5名向け「IDが変わりました」／B=未利用3名(ゆうり・りん・さくと)向け「はじめての案内」／C=保護者向け／D=塾長用の一覧メモ
>   - `ログインカード.html` … A4に8名分のカード（名前・ID・PIN・アプリURLのQRコード）。ブラウザで開いて印刷し、点線で切って手渡す用。QRにはアプリURLのみ入れID/PINは埋め込んでいない
> - ⚠️配布物は手作業で作った静的ファイル。**生徒が増減したらHTML/TXTを直接編集**（再生成スクリプトは無し）
> ■以前のセッション(2026-08-06)の変更（すべて公開済み）:
> - **ログインIDを SSG01〜SSG10 に変更**（生徒9名。student07=管理者兼テストのみ据え置き）。Auth側のメール(studentNN@ssg.local)は**変えず**、`get_login_email(p_code)` RPCで 表示ID→認証メール を解決する方式（`migrate_login_alias.sql`適用済）。index.htmlのEMAIL_DOMAIN直結は廃止。**admin.htmlは旧方式のまま＝管理ログインは student07 / 123456**
> - **塾内ポイントランキングを追加**（メニュー最下部「🏆 塾内ランキング」→s-rank画面）。今週=月曜0時JST〜の獲得XP合計順。`get_weekly_ranking()` RPC（`migrate_ranking.sql`→最終形は`migrate_enrollment.sql`で上書き）。上位3位はメダル・自分の行は紫＋「あなた」タグ。**今週0ptの生徒と管理者と退塾生は非表示**。自分が載っていない時は案内文を表示
> - **在籍/退塾フラグを追加**（`students.is_enrolled` boolean default true・`migrate_enrollment.sql`適用済）。**admin.htmlの生徒一覧に「在籍」列＝「退塾にする/在籍にもどす」ボタン**を追加（塾長が画面から操作可・確認ダイアログあり）。退塾生は ランキング非表示／一覧で薄く「(退塾)」表示／配布コピー対象外。**学習記録は消さない**。はるか(SSG10)を退塾に設定済み
> - **漢字の読み入力：送り仮名は書いても書かなくても正解**に（出題文末尾のひらがなを送り仮名とみなす`okuriganaOf()`。例「怖い」→こわい/こわ 両方OK。熟語は従来通り完全一致）。正解時はサーバーへ正式表記を送るのでポイント判定と一致
> - **クイズ終了後は単元一覧に戻る**（従来はホーム）。結果画面のボタンを「一覧へもどる」に改名し`quizReturnTo`で直前の一覧へ。英語/国語/社会すべて対応
> - **ヘッダーの「SSG学習」ロゴをタップでホームへ**（クイズ中は中断確認・ログイン前は無反応）
> ■以前のセッション(2026-07-29)の変更（すべて公開済み）:
> - **漢字 中3も投入**。光村『国語3』巻末(p306-316)を写真OCR→**本文の8単元・計234字**(握手/漢字に親しもう1/漢字に親しもう2/故郷/漢字2（漢字の造語力）/漢字に親しもう3/夏草（おくのほそ道）/アラスカとの出会い)。gradeUnitsに'中3'追加。元CSV=`国語CSV/漢字/中3/`。※p314-316「本文で扱っていない常用漢字」約90字(壱弐爵詔勅元帥…)は塾長判断で**除外**
> - **漢字の読みを4択→入力式に変更**（漢字＝国語で学年ありのときのみ。古文/英語/社会は4択のまま）。renderQuestionで qOpts に input＋「こたえる」を描画、`submitInput()`が採点。`toHira()`でカタカナ→ひらがな・空白除去して**正規化値をサーバーに送る**（submit_daily_quizは文字列完全一致で採点＝画面の○×と一致）。誤答/空欄時は正しい読みを緑表示。CSS=.ansform/.ansinput/.ansbtn/.ansfb
> - **漢字 中2も投入**。光村『国語2』巻末(p308-320)を写真OCR→中2 全396字・13単元(アイスプラネット/漢字に親しもう1/クマゼミ.../漢字に親しもう2/言葉の力/字のない葉書/漢字2（同訓・同音）/モアイは語る/扇の的/君は「最後の晩餐」.../「自分らしさ」.../走れメロス/漢字に親しもう5・漢字3)。SUBJECTS漢字catの gradeUnits に '中2' 追加。元CSV=`国語CSV/漢字/中2/`。特定1問削除は `node tools/del_row.mjs japanese "<unit>" "<grade>" "<prompt>"`
> - **国語の「漢字」を開通（中1）**。国語→漢字→**中1→単元→10問**の3階層を新設（古文はフラットのまま）。出題形式=**熟語→読み**（prompt=熟語, answer=よみ, format=vocab, grade='中1', unit=単元名。誤答は同unit+同gradeから自動生成）。出題ラベルは漢字=「この読みは？」/古文=「この意味は？」に分岐
> - **中1漢字 全390字・13単元を投入**（光村『国語1』巻末「一年生で学習した漢字」p308-320を写真OCR）。1ページ=1単元で命名。元CSV=`国語CSV/漢字/中1/<単元名>.csv`（熟語,よみ）。登録=`node tools/register_subject.mjs japanese "<単元>" "<csv>" "中1"`。検証=`node tools/check_kanji.mjs 中1 "<単元>"`
> - index.html: SUBJECTS.japaneseの漢字catを `topics:[]`→`gradeUnits:{中1:[13単元]}`。showCategoriesにisKanji分岐＋`showKanjiGrades`/`showKanjiUnits`（英語Unitと同じ正解数/総数バッジ）を追加
> - ⚠️**読みは要照合**（各字=代表熟語1つ＋標準読みをAI選定。教科書の用例と差異や漢字ID誤りの可能性）。判読不能で保留=「芯?」1字（漢字に親しもう4付近・未投入）。単元名は便宜命名でリネーム可
> ■以前のセッション(2026-07-17)の変更（すべて公開済み）:
> 1. **UIをポップ・カラフルに全面リデザイン**（明るいクリーム背景・教科ごとカラータイル[英=橙/数=青/国=桃/理=緑/社=紫/英検=赤]・大きな丸ボタン。index.htmlのCSSのみ、ロジック不変）
> 2. **中1英語を「教科書」と「これだけ」に分割**（英語→中1→[教科書/これだけ]→Unit/パート。「これだけ英単語」Part1〜8=新出単語＋重要表現447語を投入。grade=中1のまま unit=Part1〜Part8。元CSV`中1これだけCSV/`。§9a）。これだけ447語の**発音mp3も生成・公開**
> 3. **加点ルール追加「1回のクイズで6問以上正答→毎回+1点(XP+コイン・1日/Unit制限なし)」**。`migrate_score_bonus.sql`をSQLエディタで**適用済み(2026-07-17)**。結果画面の内訳・ガイドにも表示。§6
> 4. **ログインのエラー表示改善**（サーバー障害500/停止/通信不良時は「混み合っている」表示・PIN間違い400/401と区別）
> 5. **進捗バッジ修正**（Unit/パート一覧の集計を表示中のUnitだけに限定[.in('unit',list)]。全件取得だと1000行上限で中1のPart5以降が0/0になっていた）
> 6. **admin.htmlに生徒ログイン情報の閲覧・配布機能**（生徒一覧にPIN列[全員123456]＋各行「コピー」でURL+ID+PINをコピー・「全員ぶんコピー」も。admin.html冒頭の`STUDENT_PIN`/`APP_URL`定数で管理。§13）
> 7. **国語を開通**：国語→[漢字/古文]→分野→10問。漢字は準備中。古文=6分野64語(①形容詞形容動詞17 ②動詞10 ③敬語9 ④時季節8 ⑤副詞呼応10 ⑥その他10)。grade=''・unit=分野名・各分野に正解数/総語数バッジ。元CSV`国語CSV/`。汎用登録`tools/register_subject.mjs`新設。§4末尾
> ■以前のセッション（すべて公開済み）:
> 2026-07-07=UIリデザイン以前 / 2026-07-03=①英語中3投入(全学年完了) ②ポイント制度v2(2通貨XP/コイン・50pt/Lv・各種ボーナス) ③正誤フィードバック(緑○/赤×) ④英単語の発音音声(全2023語mp3) ⑤生徒student10追加/07をたくやに改名 ⑥社会「地理」228問(文205＋画像23) ⑦結果画面「もう一度」ボタン

---

## 0. 今すぐやりたいこと（次のゴール）
候補: ①**生徒へURL配布**(配布物は`Desktop/SSGログイン配布/`に作成済み。§11・§14) ②社会の歴史・公民を地理と同じ方式で一問一答投入 ③景品カタログ確定→コイン消費UI解禁 ④理科の開通。
- 英語=中1中2中3すべて投入済み。追加手順は§9。中3実績: NewHorizon3・11セクション456語(`中3英語CSV/`)
- 社会=地理228問投入済み(§4)。歴史/公民はサンプル各5問のみ。追加は写真→JSON→register_geo系スクリプト
- ⚠️**適用済みSQLマイグレーション**(順に): schema.sql / migrate_social.sql / migrate_english_topics.sql / migrate_points_v2.sql / migrate_reveal_answer.sql / migrate_question_image.sql / migrate_score_bonus.sql(2026-07-17=6問以上正答+1) / **migrate_login_alias.sql**(2026-08-06=表示ID→認証メール解決) / **migrate_ranking.sql**(2026-08-06=週間ランキング) / **migrate_enrollment.sql**(2026-08-06=在籍/退塾＋ランキング最終形。ranking.sqlを上書きするので新環境ではこちらだけでよい)。新環境で作り直す時はこの順で流す
- **migrate_math.sql**(2026-08-08=数学の開通＋出題RPCが解説を返すように) も**適用済み**。新環境で作り直す時は上の並びの最後にこれを流す
- ⚠️**音声生成のGoogleキー**は `tools/gemini_key.txt`(Git非追跡・ローカルのみ)。Cloud TTS(texttospeech)許可の制限なしキー。新単語の音声は tools/gen_audio_ctts.mjs で差分生成→audio/をpush

## 1. これは何か
個別指導塾SSG専用の学習プラットフォーム（Webアプリ）。生徒が自宅のスマホ/PCから毎日ログインして英単語を解き、得点や連続日数がポイント化されてモチベが上がる仕組み。
- 生徒用（index.html）＋ 塾長用の管理画面（admin.html）の2本立て
- データは Supabase（PostgreSQL）。フロントは静的HTML（GitHub Pagesで公開済み）

## 2. 公開URL・リポジトリ
- 生徒用: https://takuya5-stack.github.io/ssg-learning-app/
- 管理用: https://takuya5-stack.github.io/ssg-learning-app/admin.html
- GitHub（public）: https://github.com/takuya5-stack/ssg-learning-app （アカウント: takuya5-stack）
- ローカル作業フォルダ: /Users/takuya_oohashiba/Desktop/SSG学習アプリ （git初期化済み・main・origin設定済み）

## 3. Supabase接続情報
- プロジェクト: ssg-learning-app（東京 ap-northeast-1）
- Project URL: https://ewreqtxpswincicadotj.supabase.co
- publishable key（ブラウザ公開OK・RLS前提）: sb_publishable_K_YR6ghUr13Pn3yqqtvLcQ_xpe8iwCv
- ダッシュボード: https://supabase.com/dashboard/project/ewreqtxpswincicadotj
- 管理者ログイン（API登録・admin.html共通）: student07 / PIN 123456（内部メール student07@ssg.local）

## 4. 現在のコンテンツ状況（★重要）
英語は「分野(単語/文法…)」方式をやめ、**Unit別**に作り直し済み。
- **中1英語**: 728語 / 14セクション（Unit1〜8＋言語活動1,2,3,4,5,7）投入済み・公開済み
- **中2英語**: 839語 / 20セクション（Unit1〜8＋言語活動1〜11＋LetsReadMore）投入済み・公開済み
- **中3英語**: 456語 / 11セクション（Unit1〜6＋言語活動2-3・4-5・6＋LetsRead2・LetsRead3）投入済み・公開済み。元CSVは `中3英語CSV/`
- 社会: 分野順=地理/歴史/公民。**地理は受験対策一問一答を182問投入済み**（サンプル5＋新規177）。歴史/公民はサンプル各5問のみ。学年で分けない(grade='')・汎用4択
  - 地理177問は `社会CSV/地理.json`（{q,a}形式）→ `node tools/register_geo.mjs` で投入。図/地図記号依存の問題は除外し文だけで成立するものを4択化。誤答はchoices=nullで同じ地理の他の答えから自動生成
  - 追加時: 社会CSV/地理.json に足す→register_geo.mjs（既存50問超で二重投入防止ガードあり。増やすなら閾値調整 or 既存分をскип）
  - **地図記号14問（画像つき）も投入済み**（神社/寺院/学校/郵便局/交番/警察署/図書館/針葉樹林/広葉樹林/茶畑/果樹園/田/博物館）。unit=地理・prompt="この地図記号が表すものは？"・choicesは記号名で明示・image_url=img/sym_*.svg
- **画像つき問題の仕組み**（migrate_question_image.sql 適用済み）: questions に image_url 列追加。get_daily_questions が 'image' も返す。index.htmlは q.image があれば出題カードに<img>表示（qImg・130px）＋prompt小さめ。SVGは img/ に置きPages配信、image_url=相対パス
  - 地図記号14: img/sym_*.svg（黒描画・文字記号は実glyph卍/文/〒）。社会CSV/地図記号.json → tools/register_symbols.mjs（既存10超で二重防止）
  - 雨温図6・人口ピラミッド3: tools/gen_graphs.mjs が代表値からSVG生成（img/clim_*.svg, img/pyr_*.svg）。社会CSV/地理_画像.json → tools/register_geo2.mjs
- 地理は計228問（文205＋画像23）。「地図のアの海」等の実写依存は**切り抜かず文に言い換え**て追加（社会CSV/地理_追加.json）。方針: 実写切り抜きはしない
- ⚠️SVG検証はChromeヘッドレスで gallery.png にして目視（rsvg/magick無し）。Pages反映は多ファイル時1〜2分
- 国語: **開通済み**(is_active=true)。メニュー国語→**大分類[漢字/古文]**。**漢字=中1・中2・中3開通済み**(2026-07-29)：漢字→学年→単元→10問(**入力式**・熟語→読み)。中1=390字13単元／中2=396字13単元／中3=234字8単元(本文のみ)。詳細は冒頭サマリ。古文=**⓪歴史的仮名遣い65(入力式・2026-08-08追加)** ①形容詞・形容動詞17 ②動詞10 ③敬語9 ④時・季節の語8 ⑤副詞・呼応10 ⑥その他の重要語10＝計129問。grade=''・unit=分野名・choices=null(同分野から誤答自動生成)。元CSV=`国語CSV/古文N_*.csv`。追加は `node tools/register_subject.mjs japanese "<分野名>" "<csv>"`(汎用登録・slug指定・grade省略可)。index.htmlのSUBJECTS.japanese=on:true,cats:[{name:'漢字',topics:[]},{name:'古文',topics:[6分野]}]。漢字を足す時は漢字のtopicsに分野名を追加＋同名でregister。メニュー関数=showCategories→showCatTopics。古文の各分野に英語Unitと同じ「正解数/総語数」バッジ表示(showCatTopicsがgrade=''で集計・全問正解で緑)
- **数学: 中1・中2・中3の「計算」を開通済み**(2026-08-08)。数学→学年→単元→10問。全12単元395問
  - 中1(132問): 正負の数40・文字式32・一次方程式30・比例と反比例30
  - 中2(127問): 式の計算30・連立方程式32・一次関数30・角の計算35
  - 中3(136問): 式の展開36・因数分解36・平方根32・二次方程式32
  - **入力式**(答えは必ず整数)=正負の数/一次方程式/比例と反比例/連立方程式/一次関数/角の計算/二次方程式。**4択**(誤答は「ありがちな計算ミス」を人手設計)=文字式/式の計算/式の展開/因数分解/平方根。index.htmlの`MATH_INPUT_UNITS`で切り替え
  - 元データ=`数学CSV/<学年>_<単元>.json`、生成=`tools/gen_math12.mjs`(中1中2)と`tools/gen_math3.mjs`(中3)、検証=`tools/check_math.mjs`(全学年)、登録=`tools/register_math.mjs`(全ファイル自動)
  - 単元を足す手順: gen_math*.mjs に書き足す→`node tools/gen_math12.mjs`→**`node tools/check_math.mjs` でエラー0を確認**→`node tools/register_math.mjs`→index.htmlの`units[学年]`に単元名を追加(入力式なら`MATH_INPUT_UNITS`にも)→push
  - ⚠️**√やπを答えにする問題は入力式にできない**（スマホで打てない）。入力式にするなら答えが整数の範囲に限るか、入力欄に記号パレット（√ π / −）を足す実装が必要
- 理科/英検: 未開通（is_active=false）
- 元CSVは `中1英語CSV/` `中2英語CSV/` にUnitごと保存（Gitにも入っている）

## 5. 生徒画面の構造（英語）
`英語 → 学年(中1/中2/中3) → Unit を選ぶ → 10問`
- index.html の `SUBJECTS`（410行あたり）の english エントリ：
  - `gradeFirst:true, grades:true`
  - `units` は**学年ごとの連想配列**：`{ '中1':[...14...], '中2':[...20...], '中3':[] }`
  - 中3は空配列なので、いま中3を選ぶと「準備中」表示になる（`showUnits`が空を検出）
- Unit選択画面には各Unitに **「正解数/総単語数」バッジ**が出る（分子=last_result=true の語数、分母=そのUnitの総語数、全問正解は緑）。進捗は本人分のみ読み込み（RLS準拠）
- **出題中の正誤フィードバック**（migrate_reveal_answer.sql 適用済み）: get_daily_questions が正解 answer も返す→タップで 正解=緑○/誤答=赤×＋正しい答えを緑表示（pick関数）。誤答時は表示時間を長め(950ms)に。ポイントは submit_daily_quiz がサーバー側で再判定するので不正加点は不可のまま
- **英単語の音声**（2026-07-01・公開済み）: 英語vocab全2023語の発音mp3を `audio/{question_id}.mp3` に配置（GitHub Pagesで配信）。出題カードの「🔊 音声」ボタン＝出題時に自動再生＋タップで再生（英語のみ表示。id対応で引く）。音声が無い問題は無音でスルー
  - 生成: Google Cloud Text-to-Speech（声 en-US-Neural2-F）。mp3直接取得。スクリプト tools/gen_audio_ctts.mjs（Git非追跡）、対象一覧 tools/vocab_list.json、APIキー tools/gemini_key.txt（gitignore）。合計約19MB
  - 新語を足したら: register後、vocab_list更新→gen_audio_ctts.mjsで差分生成→audio/をcommit&push
  - ⚠️Gemini APIのTTSは無料100回/日で不足→Cloud TTS(別API・無料枠月100万文字)に切替済み。キーはCloud TTS許可の制限なしキー(Cloud Consoleで発行)
- ナビ関数: `renderMenu → showGradeList(学年) → showUnits(s,grade) → startQuiz(slug,unit,grade)`。社会は別ルート（showTopics/showGrades）で共存

## 6. データ構造（Supabase・要点）
- questions(id, subject_id, unit, grade, format, prompt, answer, choices, explanation, is_active)
  - 英単語は format='vocab', prompt=英語, answer=日本語, choices=null（誤答は同じ unit+grade の他の答えから自動生成）, unit='Unit1'等, grade='中1'等
- student_question_progress(student_id, question_id, correct_count, wrong_count, last_result(boolean), last_seen_date)
- daily_sessions … 各セット実施ログ（再挑戦も毎回1行。旧uniqueは撤廃）。topic="Unit/学年"
- students … total_points に加え **xp（累積・減らない=レベル）/ coin（残高=景品用）/ last_login_bonus_date** を保持。total_points は xp と同期（admin互換）
- 新テーブル: daily_unit_perfect（全問正解+5を同日同Unit1回に制御）/ unit_mastery（Unit制覇+5を一生1回）/ point_ledger（XP・コイン入出金台帳＝将来のコイン消費/引き換え待ちリストの土台。本人＋先生が閲覧可）
- RLS有効。生徒は自分の行だけ読める。加点・採点はサーバー側RPC（改ざん不可）
- 主要RPC: get_daily_questions(slug,limit,unit,grade) / submit_daily_quiz(slug,answers,unit,grade) / **claim_login_bonus()** / calc_level(points)=**50ptで1Lv**

### ポイント制度（2026-07-01 改訂・migrate_points_v2.sql 適用済み）★最新
- **2通貨**: XP(累積・レベル)＝コイン(残高・景品用) を**同額・同時**付与
- 貯め方: ログイン+1(1日1回) / 参加賞=1セット実施+1(1日1回) / **6問以上正答+1(毎回・制限なし。migrate_score_bonus.sql・2026-07-17適用)** / 10問全問正解+5(同Unit1日1回) / Unit制覇=全単語を1度以上正解+5(一生1回)
- ストリーク: 3日+5 / 7日+15 / 14日+30 / 30日+50(以降30日ごと繰返)。判定=その日に参加賞が入った(1セット実施)こと。日本時間。連続と通算(total_days)の2本立て
- レベル: 50ptごと(Lv=xp/50+1)。UIは「次まであと◯pt」表示
- 景品交換UIは**準備中(グレーアウト)**。コイン消費/引き換えは景品カタログ確定後に解禁(保管仕様: 選択式ボタンでcoinだけ減らしpoint_ledgerにkind=spendで履歴、XP不変)
- index.html: ログイン時 claim_login_bonus 呼出／結果画面にXP+コイン+内訳(参加賞/連続/全問正解/制覇)／マイページにコイン残高・景品準備中・「どうすればポイントが入る?」ガイド画面(s-guide)

## 7. 写真→CSVの変換ルール（中1・中2で確定した方針）
- 出力は `単語,意味` の1行1語CSV（admin.htmlのCSV一括にも貼れる形式）
- **全部入れる**：短縮形・固有名詞・重要表現・フレーズも含める
- **Unitごとに1ファイル**にセクション分け（Unit1〜、言語活動N、LetsReadMore など）
- 意味の区切りは日本語読点「、」を使う（ASCIIカンマは使わない）
- ⚠️**英語側にASCIIカンマを入れない**：CSV取り込みは最初のカンマまでを英単語とみなすため。
  例: `Best wishes,`→`Best wishes`、`Oh, no.`→`Oh no.`、`Go, ~!`→`Go ~!`、`~, right?`→`~ right?`、`Yes, please.`→`Yes please.` のようにカンマを除去
- ファイル名例: `Unit1_HajinsDiary.csv` / `言語活動1.csv` / `LetsReadMore.csv`
- 横向き・低解像度写真はOCR精度が落ちる。作った後に「怪しい所リスト」を出して本と照合してもらうこと（中2のLetsReadMore/言語活動11は最初ミスして再送写真で修正した実績あり）

## 8. 登録・削除スクリプト（tools/ にローカル保存・Git非追跡）
管理者権限でSupabase REST APIに直接登録する。admin.htmlを手で操作しなくてよい。
※管理者ログイン情報を含むため tools/ は .gitignore 済み（公開リポジトリには上げない）。
- 登録: `node tools/register_unit.mjs "<Unit名>" "<CSVパス>" "<学年>"`
  - 例: `node tools/register_unit.mjs "Unit1" "中3英語CSV/Unit1_xxx.csv" "中3"`
  - 同じ unit+grade が既にあると重複回避で中止する（先に削除が必要）
- 削除: `node tools/del_unit.mjs "<Unit名>" "<学年>"`（入れ替え・やり直し用）
- 中身は student07@ssg.local / 123456 で認証 → questions に insert（RLSはis_adminで許可）
- ネットワークとnodeが使えればそのまま動く（中1728語・中2839語をこれで投入済み）

## 9. 学年の英単語を追加する手順（中3もこの通り）
1. 教科書巻末「新出単語・重要表現」ページの写真をもらう（複数枚。最後に「これで全部」）
2. §7のルールで UnitごとにCSV化 → `中3英語CSV/` に保存
3. カンマ安全チェック: `grep -nE '^[^,]*, ' 中3英語CSV/*.csv`（何も出なければOK）
4. 各ファイルを登録: `node tools/register_unit.mjs "<Unit名>" "中3英語CSV/<file>" "中3"` をファイル数ぶん
5. index.html の SUBJECTS.english.units の `'中3':[]` を、実際のUnit一覧（教科書の順）に置き換え
6. 動作確認（下記の検証コマンド）
7. デプロイ: `git add -A && git commit -m "..." && git push`（tools/はignoreされるので入らない）
8. 反映後（1分）、公開URLで 英語→中3→Unit を実機確認

### 9a. 「これだけ」パートの追加・やり直し（中1）
- 元CSV: `中1これだけCSV/Part1.csv`〜`Part8.csv`（形式=§7と同じ `英単語,意味`）
- メニュー: index.html の SUBJECTS.english に `korekore:{'中1':['Part1'..'Part8']}`。中1を選ぶと `showCollections`→教科書/これだけ、これだけで `showUnits(...,{units:korekore,label:'これだけ',noun:'パート'})`
- 登録: `node tools/register_unit.mjs "Part1" "中1これだけCSV/Part1.csv" "中1"`（grade=中1・unit=PartN。教科書のUnit名と衝突しない）
- やり直し: `node tools/del_unit.mjs "Part1" "中1"` で削除→再登録
- ⚠️同一パート内で「答え(意味)の文字列」が完全一致すると4択に同じ選択肢が2つ出る。a/an・listen・able などは表記をずらして回避済み
- 収録方針: 各Partの「新出単語＋重要表現」のみ。数字/月/季節/序数/あいさつのおまけ単語は今回は未収録（入れるなら同PartのCSVに追記→del&再登録）
- ✅音声mp3=生成済み(447件・Cloud TTS Neural2-F・audio/にpush済み・公開配信OK)。追加した時は `node tools/build_kore_list.mjs`(これだけ全単語をvocab_list.jsonに追記)→`node tools/gen_audio_ctts.mjs`(差分生成)→`git add audio/ && push`

### 検証コマンド例（登録数とRPC出題の確認）
同内容を `check.mjs` に書いて `node check.mjs` で実行（インライン `node -e` だと環境により `URL is not a constructor` エラーになることがある）:
```js
const U='https://ewreqtxpswincicadotj.supabase.co',K='sb_publishable_K_YR6ghUr13Pn3yqqtvLcQ_xpe8iwCv';
const t=(await fetch(U+'/auth/v1/token?grant_type=password',{method:'POST',headers:{apikey:K,'Content-Type':'application/json'},body:JSON.stringify({email:'student07@ssg.local',password:'123456'})}).then(r=>r.json())).access_token;
const H={apikey:K,Authorization:'Bearer '+t};
const sid=(await fetch(U+'/rest/v1/subjects?slug=eq.english&select=id',{headers:H}).then(r=>r.json()))[0].id;
const rows=await fetch(U+'/rest/v1/questions?subject_id=eq.'+sid+'&grade=eq.'+encodeURIComponent('中3')+'&select=unit',{headers:H}).then(r=>r.json());
const c={};rows.forEach(r=>c[r.unit]=(c[r.unit]||0)+1); console.log('中3合計:'+rows.length, c);
```

## 10. デプロイ手順
HTMLやCSVを直したら:
```
git -C "/Users/takuya_oohashiba/Desktop/SSG学習アプリ" add -A
git -C "/Users/takuya_oohashiba/Desktop/SSG学習アプリ" commit -m "更新内容"
git -C "/Users/takuya_oohashiba/Desktop/SSG学習アプリ" push
```
→ GitHub Pagesが自動再ビルド（1分ほどで反映）

## 11. 登録済み生徒（PINは全員 123456）
生徒のログインID=**students.student_code**（2026-08-06にSSG形式へ変更）。裏の認証メールは studentNN@ssg.local のままで、`get_login_email` RPCが 表示ID→メール を解決する。
- **SSG01 樹(中1) / SSG02 和己(中2) / SSG03 吏絆(中2) / SSG04 梨香子(中3) / SSG05 かりん(中3) / SSG06 ゆうり(高1) / SSG08 りん(高1) / SSG09 さくと(高1) / SSG10 はるか(中2・退塾)**
- **student07 たくや(中1) = 管理者兼テスト用**（admin.htmlログイン・API登録もこれ。IDは変更していない＝`student07`のまま）
- IDを変えたい時: `students.student_code` を書き換えるだけ（Auth側は触らない）。`tools/rename_to_ssg.mjs` が雛形
- **退塾**: admin.htmlの生徒一覧「在籍」列のボタンで切替（is_enrolled）。ランキング非表示・配布対象外になるが記録は残る
- 生徒追加手順: ①Authentication→Add user→Create new user（Email studentNN@ssg.local / Password=PIN / Auto Confirm ON）②SQL Editorで students に insert（add_student10.sql が雛形。student_codeはSSGNNにする）。※新規Authユーザー作成はservice_roleキーが要る＝ダッシュボード手動。REST公開キーでは不可

## 12. 既知の注意点・セキュリティ
- ⚠️**セキュリティ**: このpublicリポジトリと本書に管理者ログイン(student07/123456)が載っている。誰でもadmin.htmlに入れてしまう。気にするなら「管理者PINを変更」「リポジトリをprivate化」を検討（tutoring用途で低リスクと判断して現状維持中）
- PINが全員123456。他人のIDが分かると入れる。生徒ごとに変更も可（Supabase Authentication）
- SupabaseのSQLエディタはChromeのページ翻訳オンだと壊れる→翻訳オフで
- ローカルのブラウザ実機確認は環境都合で不可だった（プレビューサーバーが日本語パスを配信できず＋要ログイン）。実表示確認は公開URLで生徒ログインして行う。データ層はAPIで検証できる
- submit_daily_quizのtotal_days返り値は更新前値を返す軽微なバグ（DB値は正しい・表示影響なし）

## 13. ファイル構成
- index.html … 生徒用（ログイン→教科→[学年/大分類/分野]→10問→採点/ポイント/進捗バッジ）
- admin.html … 塾長用（問題登録[英単語型CSV一括/汎用4択]・生徒一覧＋**生徒ログイン情報の閲覧/配布コピー**[PIN列・コピー/全員コピー]＋**在籍列=退塾/在籍の切替ボタン**。冒頭定数`STUDENT_PIN="123456"`/`APP_URL`）
- 中1英語CSV/ , 中2英語CSV/ , 中3英語CSV/ … 教科書の単語CSV（Unitごと）
- **中1これだけCSV/** … これだけ Part1〜8（新出単語＋重要表現）
- **国語CSV/** … 古文単語 6分野（古文N_*.csv）＋ **古文_歴史的仮名遣い.json**（{prompt,answer,explanation,rule,post?,yoon?,manual?}）＋ **漢字/中1・中2・中3/**（単元ごと `熟語,よみ`）
- **数学CSV/** … 数学の計算12単元 `<学年>_<単元>.json`（{prompt, answer, choices, explanation, check?}。choices=null は入力式。checkは検証モード指定）
- tools/ … register_unit.mjs, del_unit.mjs, **register_subject.mjs**(汎用登録), **del_row.mjs**(1問だけ削除), **check_kanji.mjs**(漢字の出題/件数確認), **list_students.mjs**(生徒一覧), **rename_to_ssg.mjs**(ID一括変更), **gen_math3.mjs**(中3数学の生成), **gen_math12.mjs**(中1中2数学の生成), **check_math.mjs**(数学の数値検証・全学年), **register_math.mjs**(数学の登録・全ファイル自動), **check_kana.mjs**(歴史的仮名遣いのルール照合), **register_kana.mjs**(歴史的仮名遣いの登録), build_kore_list.mjs, gen_audio_ctts.mjs（ローカル限定・Git非追跡）
- schema.sql / migrate_social.sql / migrate_english_topics.sql / **migrate_points_v2.sql** / **migrate_reveal_answer.sql** / migrate_question_image.sql / **migrate_score_bonus.sql** / **migrate_login_alias.sql** / **migrate_ranking.sql** / **migrate_enrollment.sql** … 初期スキーマ＋マイグレーション（すべて適用済み）
- register_students.sql / seed_test_words.sql / add_student10.sql … 生徒登録・初期単語

## 14. 残タスク
1. **新ID(SSG01〜)を生徒へ再配布** ← **配布物は作成済み(2026-08-07)。あとは塾長が渡すだけ**。`Desktop/SSGログイン配布/配布文面.txt`(LINE用の文面)と`ログインカード.html`(A4印刷・QR付き)。旧student01等はもう使えないので既存5名には「IDが変わった」案内が要る。未利用=ゆうり/りん/さくとの3名。※admin.htmlの「コピー/全員ぶんコピー」でも同じ情報は出せる
2. 景品カタログ・価格の確定 → コイン消費/引き換えUIを解禁（保管仕様は§6ポイント制度に記載）
3. （任意）PINを生徒ごとに変更・セキュリティ向上
3b. （任意）**ランキングの集計からログインボーナスを除外するか**（現状はログインだけの+1ptでもランキングに載る。「実際に解いた生徒だけ」にするなら get_weekly_ranking の合計を `pl.kind <> 'login'` で絞る）。**退塾生のログイン自体は今も可能**（塞ぐなら別途対応）
4. 国語=古文（**歴史的仮名遣い65問を追加**・2026-08-08）＋**漢字(中1・中2・中3)開通済み**(2026-07-29・入力式)。残：①(任意)歴史的仮名遣いの逆（現代→歴史的）や文単位の追加 ①漢字の読みを本と照合(AI選定・中1保留字「芯?」1字あり) ②書き取り(読み→漢字)形式 ③(任意)中3「本文で扱っていない常用漢字」約90字
4b. **数学=中3の計算のみ開通済み**(2026-08-08・解説パネルも稼働中)。残：①**中1・中2の計算を追加**（正負の数/文字式/一次方程式/比例反比例・式の計算/連立方程式/一次関数）＝gen_math3.mjsに書き足すだけ ③(任意)入力欄に記号パレット(√ π / −)を足して平方根や二次方程式の無理数解も入力式にする ④(任意)公式・図形の性質の4択単元。理科/英検は未開通
5. 社会の歴史/公民をサンプル5問→本格投入
6. ランキングは**実装済み**(2026-08-06)。残りは admin側の引き換え待ちリスト画面（point_ledgerのkind=spend）
